[package]
name = "poker-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic poker rules engine: cards, evaluation, variants, hand state machine."

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true, optional = true }

[features]
serde = ["dep:serde"]
