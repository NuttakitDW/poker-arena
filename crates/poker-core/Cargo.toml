[package]
name = "poker-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic poker rules engine: evaluation, variants, hand state machine."

[dependencies]
poker-wire = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true, optional = true }

[dev-dependencies]
criterion = { workspace = true }

[features]
serde = ["dep:serde"]

[[bench]]
name = "eval"
harness = false

[[bench]]
name = "engine"
harness = false
